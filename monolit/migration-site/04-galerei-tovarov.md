# 4. Галереи товаров

После переноса на сервер необходимо импортировать галереи из `../public_html/assets/images/products`.

> ⚠️ **Важно:** Перед началом работы проверьте, изменились ли ID товаров после переноса. Это определит дальнейшие действия.

## Проверка изменения ID товаров

Перед переносом галерей необходимо проверить, изменились ли ID товаров. Сравните несколько ID товаров на старом и новом сайте по их URI (алиасам).

### Как проверить

1. Выберите несколько товаров на старом сайте и запомните их ID и URI
2. Найдите эти же товары на новом сайте по URI
3. Сравните ID

**Если ID совпадают** → переходите к [сценарию без изменения ID](#сценарий-1-id-не-изменились)

**Если ID не совпадают** → переходите к [сценарию с изменением ID](#сценарий-2-id-изменились)

---

## Сценарий 1: ID не изменились

Если ID товаров остались прежними, процесс упрощается.

### Шаг 1: Создание файла include.txt

Создайте файл `include.txt` со следующей структурой:

<details>
<summary>Показать код</summary>

```txt
    + /assets/
    + /assets/images/
    + /assets/images/products/
    + /assets/images/products/12345/
    + /assets/images/products/10000/
    + /assets/images/products/54321/
    - *
```

</details>

> 💡 **Примечание:** Замените `12345`, `10000`, `54321` на реальные ID товаров, галереи которых нужно перенести.

### Шаг 2: Запуск rsync

Запустите команду rsync с использованием файла include.txt:

<details>
<summary>Показать код</summary>

```bash
rsync -av --include-from=include.txt \
/home/minvata/web/path/public_html/ \
root@193.42.125.65:/home/user/web/new_path/public_html/
```

</details>

### Шаг 3: Проверка

После переноса проверьте:

- [ ] Файлы галерей находятся в правильных папках
- [ ] Записи в таблице `modx_ms2_product_files` соответствуют ID товаров
- [ ] Галереи отображаются корректно на сайте

---

## Сценарий 2: ID изменились

Если ID товаров изменились после переноса, необходимо обновить:

1. Названия папок галерей
2. Записи в таблице `modx_ms2_product_files`

### Шаг 1: Получение списка ID товаров

Сначала получите список всех товаров с их URI (для сопоставления старых и новых ID):

```sql
SELECT id, uri FROM modx_site_content
WHERE class_key = 'msProduct'
ORDER BY id
```

### Шаг 2: Создание маппинга ID

Создайте скрипт для создания маппинга старых ID → новых ID по URI:

<details>
<summary>Показать код</summary>

```php
<?php
/**
 * Скрипт для создания маппинга старых ID → новых ID
 * Запускается на новом сайте
 * Использует URI для сопоставления
 */

// Получаем все товары с их URI
$products = $modx->getCollection('msProduct',['context_key' => 'CONTEXT']);
$mapping = [];

foreach ($products as $product) {
    $mapping[$product->get('uri')] = $product->get('id');
}

// Сохраняем маппинг
file_put_contents(
    MODX_BASE_PATH . 'product_id_mapping.json',
    json_encode($mapping, JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT)
);

echo "Маппинг создан. Всего товаров: " . count($mapping);
```

</details>

### Шаг 3: Перенос галерей с переименованием папок

После создания маппинга перенесите галереи и переименуйте папки:

<details>
<summary>Показать код</summary>

```php
<?php
/**
 * Скрипт переноса галерей с переименованием папок
 * Запускается на новом сайте после переноса файлов
 */

$mappingFile = MODX_BASE_PATH . 'product_id_mapping.json';
$oldMappingFile = MODX_BASE_PATH . 'old_product_id_mapping.json'; // Старые ID → URI

$newMapping = json_decode(file_get_contents($mappingFile), true);
$oldMapping = json_decode(file_get_contents($oldMappingFile), true); // URI → старый ID

$productsPath = MODX_BASE_PATH . 'assets/images/products/';

// Создаем обратный маппинг: старый ID → новый ID
$idMapping = [];
foreach ($oldMapping as $uri => $oldId) {
    if (isset($newMapping[$uri])) {
        $idMapping[$oldId] = $newMapping[$uri];
    }
}

// Переименовываем папки
foreach ($idMapping as $oldId => $newId) {
    $oldPath = $productsPath . $oldId;
    $newPath = $productsPath . $newId;

    if (is_dir($oldPath) && !is_dir($newPath)) {
        rename($oldPath, $newPath);
        echo "Переименовано: $oldId → $newId\n";
    }
}
```

</details>

### Шаг 4: Обновление таблицы modx_ms2_product_files

Обновите записи в таблице `modx_ms2_product_files`:

<details>
<summary>Показать код</summary>

```php
<?php
/**
 * Обновление product_id в modx_ms2_product_files
 */

$mappingFile = MODX_BASE_PATH . 'product_id_mapping.json';
$oldMappingFile = MODX_BASE_PATH . 'old_product_id_mapping.json';

$newMapping = json_decode(file_get_contents($mappingFile), true);
$oldMapping = json_decode(file_get_contents($oldMappingFile), true);

// Создаем маппинг: старый ID → новый ID
$idMapping = [];
foreach ($oldMapping as $uri => $oldId) {
    if (isset($newMapping[$uri])) {
        $idMapping[$oldId] = $newMapping[$uri];
    }
}

// Обновляем таблицу
foreach ($idMapping as $oldId => $newId) {
    $modx->query("
        UPDATE modx_ms2_product_files
        SET product_id = {$newId}
        WHERE product_id = {$oldId}
    ");
    echo "Обновлено: $oldId → $newId\n";
}

echo "Готово!";
```

</details>

### Шаг 5: Проверка

После выполнения всех шагов проверьте:

- [ ] Все папки галерей переименованы правильно
- [ ] Записи в `modx_ms2_product_files` обновлены
- [ ] Галереи отображаются корректно на сайте
